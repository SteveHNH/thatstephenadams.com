---
title: "Posts"
# The Mana theme lists content where .Type == "posts". Cascade type: posts onto
# every regular page in this section (kind: page only, so this _index.md keeps
# type: post and the section list template lookup stays layouts/post/list.html).
# URLs are unchanged because the section name is still "post".
cascade:
  - target:
      kind: page
    type: posts
---
